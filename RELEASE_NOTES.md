## What's New in 1.2.2

### Changed
- Require Obsidian **1.13.0+**
- Settings use only the declarative `getSettingDefinitions()` API (removed deprecated `display()`)

---

## 1.2.2 更新内容

### 变更
- 最低要求 Obsidian **1.13.0+**
- 设置页仅使用声明式 `getSettingDefinitions()`（移除已弃用的 `display()`）
